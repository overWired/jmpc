description = 'the controller layer of the application'

dependencies {
    compile  project(':jmpc-business-service')
    compile  project(':jmpc-domain-view')
    compile('org.springframework:spring-web')
}
